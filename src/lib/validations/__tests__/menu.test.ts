import { describe, it } from 'vitest';

describe('menuItemSchema', () => {
  describe('正常系', () => {
    // 必須フィールドのみで有効なデータ
    it.todo('名前・価格・カテゴリIDを指定した場合、バリデーションが成功すること');

    // 任意フィールドを含む有効なデータ
    it.todo('説明を含めた場合でも、バリデーションが成功すること');

    // 説明が省略可能であること
    it.todo('説明が未指定の場合、バリデーションが成功すること');
  });

  describe('name バリデーション', () => {
    // 空文字は拒否
    it.todo('名前が空文字の場合、エラーメッセージが返ること');

    // 上限ちょうどはOK
    it.todo('名前が50文字ちょうどの場合、バリデーションが成功すること');

    // 上限超過は拒否
    it.todo('名前が51文字の場合、エラーメッセージが返ること');
  });

  describe('price バリデーション', () => {
    // 最小値（1円）はOK
    it.todo('価格が1の場合、バリデーションが成功すること');

    // 0は拒否
    it.todo('価格が0の場合、エラーメッセージが返ること');

    // 負の数は拒否
    it.todo('価格が負の数の場合、エラーメッセージが返ること');

    // 小数は拒否
    it.todo('価格が小数の場合、エラーメッセージが返ること');

    // 上限ちょうど（100万円）はOK
    it.todo('価格が1000000の場合、バリデーションが成功すること');

    // 上限超過は拒否
    it.todo('価格が1000001の場合、エラーメッセージが返ること');
  });

  describe('category_id バリデーション', () => {
    // 有効なUUID
    it.todo('有効なUUID形式の場合、バリデーションが成功すること');

    // 無効な文字列
    it.todo('UUID形式でない文字列の場合、エラーメッセージが返ること');

    // 空文字
    it.todo('空文字の場合、エラーメッセージが返ること');
  });

  describe('description バリデーション', () => {
    // 上限ちょうどはOK
    it.todo('説明が500文字ちょうどの場合、バリデーションが成功すること');

    // 上限超過は拒否
    it.todo('説明が501文字の場合、エラーメッセージが返ること');
  });
});

describe('categorySchema', () => {
  describe('正常系', () => {
    // 有効なカテゴリ名
    it.todo('カテゴリ名を指定した場合、バリデーションが成功すること');
  });

  describe('name バリデーション', () => {
    // 空文字は拒否
    it.todo('カテゴリ名が空文字の場合、エラーメッセージが返ること');

    // 上限ちょうどはOK
    it.todo('カテゴリ名が50文字ちょうどの場合、バリデーションが成功すること');

    // 上限超過は拒否
    it.todo('カテゴリ名が51文字の場合、エラーメッセージが返ること');
  });
});
